type User {
  id: ID
  name: String!
  email: String
  phoneNumber: String
  street: String
  city: String
  state: String
}

input UserInput {
  name: String!
}

type Query {
  onePerson(id: ID!): User
  getAllUsers: [User]
}

type Mutation {
  addUser(input: UserInput!): User!
}
